<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.seckill.mapper.SeckillOrderMapper">

    <resultMap id="BaseResultMap" type="com.example.seckill.entity.SeckillOrder">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="product_id" property="productId" jdbcType="BIGINT"/>
        <result column="order_id" property="orderId" jdbcType="VARCHAR"/>
        <result column="seckill_price" property="seckillPrice" jdbcType="DECIMAL"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, product_id, order_id, seckill_price, status, create_time
    </sql>

    <insert id="insert" parameterType="com.example.seckill.entity.SeckillOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO seckill_order (user_id, product_id, order_id, seckill_price, status, create_time)
        VALUES (#{userId,jdbcType=BIGINT}, #{productId,jdbcType=BIGINT}, #{orderId,jdbcType=VARCHAR},
                #{seckillPrice,jdbcType=DECIMAL}, #{status,jdbcType=INTEGER}, #{createTime,jdbcType=TIMESTAMP})
    </insert>

    <select id="selectByUserIdAndProductId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE user_id = #{userId,jdbcType=BIGINT}
          AND product_id = #{productId,jdbcType=BIGINT}
        LIMIT 1
    </select>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE order_id = #{orderId,jdbcType=VARCHAR}
        LIMIT 1
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE user_id = #{userId,jdbcType=BIGINT}
        ORDER BY create_time DESC
    </select>

    <select id="selectByStatusAndCreateTimeBefore" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE status = #{status,jdbcType=INTEGER}
          AND create_time &lt; #{deadline,jdbcType=TIMESTAMP}
    </select>

    <update id="updateStatus">
        UPDATE seckill_order
        SET status = #{status,jdbcType=INTEGER}
        WHERE order_id = #{orderId,jdbcType=VARCHAR}
    </update>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order
        ORDER BY create_time DESC
    </select>

</mapper>
